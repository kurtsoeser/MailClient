import { RulesShell } from '@/app/rules/RulesShell'

/** Einstellungen → Mail → Regeln (eigener Chunk). */
export default function AccountSetupRulesPanel(): JSX.Element {
  return <RulesShell embedded />
}
